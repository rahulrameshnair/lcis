"""LCIS package metadata.

The package exposes the Life Cycle Inventory Schema tooling implemented under
``lcis.inventory`` and ``lcis.imports``. This top-level module currently keeps
the package version only and has no runtime dependencies.
"""

__version__ = "0.3.0"
